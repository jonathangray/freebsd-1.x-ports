copy item (number is c6,
	name is c20,
	dept is c6,
	price is c6,
	qoh is c6,
	supplier is c6)
into "{pathname}/ndemo/item"

copy sale (
	number is c6,
	date is c8,
	store is c6,
	dept is c6,
	item is c6,
	quantity is c6,
	employee is c6,
	credit is c8)
into "{pathname}/ndemo/sale"

copy employee (
	number is c6,
	name is c20,
	salary is c6,
	manager is c6,
	birthdate is c6,
	startdate is c6)
into "{pathname}/ndemo/employee"

copy dept (
	number is c6,
	name is c20,
	store is c6,
	floor is c6,
	manager is c6)
into "{pathname}/ndemo/dept"

copy supplier (
	number is c6,
	name is c15,
	city is c15,
	state is c6)
into "{pathname}/ndemo/supplier"

copy store (
	number is c6,
	city is c15,
	state is c6)
into "{pathname}/ndemo/store"

copy parts (
	pnum is c6,
	pname is c20,
	color is c8,
	weight is c6,
	qoh is c6)
into "{pathname}/ndemo/parts"

copy supply (
	snum is c6,
	pnum is c6,
	jnum is c6,
	shipdate is c8,
	quan is c6)
into "{pathname}/ndemo/supply"
