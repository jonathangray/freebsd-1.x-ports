To install on FreeBSD, it should be enough to :
  # make clean all
  # su root
  # make install

To configure top for another machine, or build it using a configuration
other than the default:
  # make veryclean
  # ./Configure
  ...
