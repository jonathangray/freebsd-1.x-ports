Changes made to sc  for FreeBSD:
Changed encryption program to bdes(1)
Changed default pager to more(1)
Added regex(3) support

If you  are used to the Lotus 1-2-3 interface, you might look at the ss
spreadsheet package, which is derived from sc.
