This directory contains the source code of ups: an X based C and
FORTRAN debugger.

The user interface of ups is described in the manual page, which is
in the file doc/ups.man.  The file doc/porting.ms contains a guide
to porting ups to a new architecture.  It also describes the general
layout of the source.
