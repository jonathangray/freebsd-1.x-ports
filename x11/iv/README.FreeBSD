To build this, do the following (assuming a shell of sh or bash):

	CPU=FREEBSD make

To remake the Makefiles from the Imakefiles, should that ever be necessary,
do this:

	imake -I`pwd`/src/config -T`pwd`/src/config/InterViews/template \
	  -DTOPDIR=. -DCURDIR=.
	make Makefiles
