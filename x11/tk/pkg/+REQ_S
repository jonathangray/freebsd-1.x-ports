#!/bin/sh
if pkg_info -e tcl_src; then
	:;
else
	echo > /dev/tty
	echo "Warning:  tcl package is not installed.  You should install the tcl_bin" > /dev/tty
	echo "package as well before attempting to use this tk distribution." > /dev/tty
	echo > /dev/tty
fi
exit 0
