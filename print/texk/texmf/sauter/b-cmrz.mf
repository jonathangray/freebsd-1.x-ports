% Computer Modern Cyrillic
if unknown cmbase: input cmbase fi

font_identifier:="CMRZ"; font_size design_size*pt#;

input c-cmr;   % compute size parameters for CMR family

generate cyrillic   % switch to the driver file
