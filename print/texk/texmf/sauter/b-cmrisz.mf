% Computer Modern Cyrillic Slanted Sans Serif
if unknown cmbase: input cmbase fi

font_identifier:="CMCSSI"; font_size design_size * pt#;

% This font is a simple modification of CMSS.

input c-cmss;

slant:=sind 12/cosd 12;    % tilt ratio $(\Delta x/\Delta y)$

generate cyrillic      % switch to the driver file

