       frename oldPath newPath
              Renames  oldPath to newPath.  This command does not
              support renaming across  file  systems  unless  the
              rename  system  call  supports  them.   If renaming
              accross file systems is desired, exec the  mv  com-
              mand.

              This command is provided by Extended Tcl.
