(* Copyright (c) 1992 by Carnegie Mellon University *)

(* Gene Rollins (rollins+@cs.cmu.edu)
   School of Computer Science, Carnegie-Mellon Univ, Pittsburgh, PA *)

(* (sort op lst) Uses the mergesort algorithm to sort the list lst.
   Returns the list sorted according to the comparison operator op.
   No side effects. *)

(* (unique'sort equal op lst) Uses the mergesort algorithm to sort lst.
   Returns the list sorted according to the comparison operator op.
   Duplicate elements (according to equal) are eliminated, and the resulting
   list has unique elements. No side effects. *)
