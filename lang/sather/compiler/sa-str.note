
Currently, the implementation is such that if we have
     foo is
        OUT::s("bar").s("bar");
     end;
then in the generated C code, there is only declaration of string
"bar".  

NOTE: To provide a compiler option that will generate separate
declarations for the two instances of "bar".
