FreeBSD systems are generic BSD systems. No additional
modifications to the Makefile are needed. Under 1.1, if you want to
use dynamic loading you need to add

	-static

to the ULDFLAGS variable in the Makefile to disable shared labraries.

