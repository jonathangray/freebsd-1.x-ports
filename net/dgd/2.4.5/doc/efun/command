int command(str, ob)
string str;
object ob;

Execute "str" as a command given directly by the player.
Any effects of the command will apply to the current object.
If the second optional argument is present, thne the command is executed
for that object.

Return value is 1 or 0, for success or failure.

See also enable_commands().
