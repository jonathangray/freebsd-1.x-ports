#!/bin/sh
if [ ! -f /usr/local/bin/dpwish ]; then
	echo "This package requires that dpwish from the tcl-dp package"
	echo "be installed in /usr/local/bin.  Install the tcl-dp package"
	echo "first before trying to install the binary package of zircon."
	exit 1
fi
