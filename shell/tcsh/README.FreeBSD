tcsh-6.04

using ./config/config.bsd4.4 for config.h

To build and install from orignal sources:
ln -s ./config/config.bsd4.4 config.h  #create's symlink for config.h

cp Makefile.std Makefile.

edit Makefile for your options although you can compile without changing
anything

make
make install

To build and install from these sources:

make
make install

--
smace@freefall.cdrom.com
