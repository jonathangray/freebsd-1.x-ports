tcsh-6.04

to build:
ln -s ./config/config.bsd4.4 config.h  #create's simlink for config.h

cp Makefile.std Makefile.

edit Makefile for your options although you can compile without changing
anything

make
make install

smace@freefall.cdrom.com
