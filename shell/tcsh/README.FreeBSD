Note for 'vi command mode' lovers:
"bind" command now obsoleted per author intention,
use "bindkey -v" instead.

This stuff is pre-configured by:
1) cp config/bsd4.4 config.h
2) cp Makefile.std Makefile and editing it

Just type:
	make
	make install

Ache.
